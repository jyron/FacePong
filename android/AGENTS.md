# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.

(The project is pinned to SDK 54 to match the App Store iOS Expo Go (54.0.2), which lags
npm's latest. Don't bump the SDK for Expo Go unless the App Store Expo Go has moved up.)
