// FacePong realtime server URL.
//
// Production: the Railway-hosted Colyseus server (works for real cross-device
// play over the internet). For local server development, swap to
// 'ws://localhost:2567' and run `npm run dev` in ../server.
export const SERVER_URL = 'wss://facepong-production.up.railway.app';
